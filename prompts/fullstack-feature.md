# Gabarit — Feature full-stack (mode Code, après validation du plan)

```
Mode : Code
R : Développeur full-stack senior sur SupportOps Copilot.
E : Implémente la feature « <NOM_FEATURE> » selon le plan validé ci-dessus.
    Livrables observables :
      - Backend : route(s) <…> avec validation payload + erreurs explicites.
      - Frontend : <écran/composant> branché + état vide/chargement/erreur.
      - Tests : Vitest backend (happy + erreur). Build front qui passe.
C : Fichiers autorisés à modifier : <LISTER>. AGENTS.md et docs/architecture.md
    font foi pour les conventions et les contrats.
C : - npm test --workspace apps/api doit passer.
    - npm run build --workspace apps/web doit passer.
    - Pas de console.log livré.
    - Résumé final du diff avec risques restants.
```
